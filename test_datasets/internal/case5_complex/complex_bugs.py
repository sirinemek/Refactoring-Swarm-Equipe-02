# Fichier avec multiples problèmes
import sys

def process(data)  # Manque :
    result = []
    for item in data:
        if item > 0
            result.append(item * 2)
    return result

class Calculator:
    def divide(self, a, b):
        return a / b
    
    def factorial(self, n):
        if n == 0:
            return 0
        return n * self.factorial(n - 1)